import { paraQuilogramas } from './paraQuilograma.pipe';

describe('TraduzPesoPipe', () => {
  it('create an instance', () => {
    const pipe = new paraQuilogramas();
    expect(pipe).toBeTruthy();
  });
});
