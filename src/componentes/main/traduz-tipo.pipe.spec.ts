import { TraduzTipoPipe } from './traduz-tipo.pipe';

describe('TraduzTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new TraduzTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
